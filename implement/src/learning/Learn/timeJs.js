const intervalId = window.setInterval(() => {
    console.log('I am interval');
    }, 500, "inter");
    
    const timeId= window.setTimeout(() => {
    console.log('I am timeId');
    }, 500, "timeId");